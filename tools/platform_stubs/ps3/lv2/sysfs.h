/* Empty on purpose. defines/ps3_defines.h includes <lv2/sysfs.h>
 * unconditionally, so it has to exist for anything to parse against
 * these stubs; nothing compiled against them uses it yet. Declare what
 * a file needs as one arrives. */
#ifndef PS3STUB_LV2_SYSFS_H
#define PS3STUB_LV2_SYSFS_H

#include <stdint.h>

#endif
