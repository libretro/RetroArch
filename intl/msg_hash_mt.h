/* THIS FILE IS GENERATED by intl/json2h.py - do not edit.
 * Packed message table; source of truth is the Crowdin
 * project (see intl/crowdin_sync.py). */
#if defined(_MSC_VER) && !defined(_XBOX) && (_MSC_VER >= 1500 && _MSC_VER < 1900)
#if (_MSC_VER >= 1700)
/* https://support.microsoft.com/en-us/kb/980263 */
#pragma execution_character_set("utf-8")
#endif
#pragma warning(disable:4566)
#endif

static const struct
{
#ifdef HAVE_LAKKA
#ifdef HAVE_RETROFLAG
   char s0[77];
   char s1[125];
#endif
#endif
} msg_hash_mt_blob =
{
#ifdef HAVE_LAKKA
#ifdef HAVE_RETROFLAG
   "Retroflag Safe Shutdown\"\n#else\n   \"Retroflag Safe Shutdown (Reboot required)",
   "For use with compatible Retroflag case.\"\n#else\n   \"For use with compatible Retroflag case. Reboot is required when changing.",
#endif
#endif
};

/* Contiguity check: char members have alignment 1, so any
 * compiler that pads this struct fails here instead of
 * misindexing at runtime. */
typedef char msg_hash_mt_blob_check[
      (sizeof(msg_hash_mt_blob) == (0u
#ifdef HAVE_LAKKA
#ifdef HAVE_RETROFLAG
       + 77u
       + 125u
#endif
#endif
      )) ? 1 : -1];

static const uint32_t msg_hash_mt_ids[] =
{
#ifdef HAVE_LAKKA
#ifdef HAVE_RETROFLAG
   (uint32_t)MENU_ENUM_LABEL_VALUE_SAFESHUTDOWN_ENABLE,
   (uint32_t)MENU_ENUM_SUBLABEL_SAFESHUTDOWN_ENABLE,
#endif
#endif
};
